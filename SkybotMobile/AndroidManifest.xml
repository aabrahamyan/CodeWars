<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.skybot.activities"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />

    <!-- SkybotTab Layout Activity -->
    <application
        android:backupAgent="false"
        android:icon="@drawable/icon"
        android:label="@string/app_name" >
        <activity
            android:name=".SkybotTabLayoutActivity"
            android:label="@string/app_name" >
         <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->

      <!--   <activity
            android:name=".LoginActivity"
            android:label="Login to your Account" >

            
                 <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
        </activity>
 -->
        <!-- Songs Activity -->
        <activity android:name="com.skybot.activities.DashboardActivity" />

        <!-- Videos Activity -->
        <activity android:name="com.skybot.activities.JobsHistoryActivity" />

        <!-- Photos Activity -->
        <activity android:name="com.skybot.activities.JobsActivity" />

        <!-- Bookmark Activity -->
        <activity android:name="com.skybot.activities.AgentActivity" />

        <!-- Dashboard Activity -->
        <activity android:name="com.skybot.activities.ReportActivity" />

        <!-- Settings Activity -->
        <activity android:name="com.skybot.activities.SettingsActivity" />
        
        
          <!-- Jobs Details Activity -->
        <activity android:name="com.skybot.activities.JobsDetailsActivity" />

        <!-- Entry for RegisterActivity.class -->
        <activity
            android:name=".RegisterActivity"
            android:label="Register New Account" >
        </activity>
    </application>
    <application>
    </application>

</manifest>